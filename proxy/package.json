{
  "name": "likho-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that proxies Likho desktop app calls to the Gemini API. Day 4: hardcoded API key, no auth — auth comes Day 8.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
